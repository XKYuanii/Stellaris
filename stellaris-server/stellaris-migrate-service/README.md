# 已退出运行主线的历史迁移服务

`stellaris-migrate-service` 保存旧订单分库分表阶段的数据迁移与 Hint 路由实现，仅用于说明架构演进。

当前交易链路使用单个 `stellaris_trade` 数据库，以便请求幂等、最终限购、座位 CAS 和建单共享一个 MySQL 本地事务。本模块因此不在父 Maven reactor、Gateway 路由、默认启动清单或部署拓扑中，也不参与当前版本的编译、测试与运行。

面试中可以用这里的代码解释旧方案如何迁移分片数据，以及为什么当前版本优先选择明确的本地事务边界。不要把本模块描述为在线服务或当前扩容方案。
