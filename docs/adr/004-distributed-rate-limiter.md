# ADR 004：分布式令牌桶与本机并发隔离分离

- 状态：已采用

网关的 `ConcurrencyBulkhead` 只保护本实例下游并发。QPS 规则使用 Redis Hash + 小 Lua Token Bucket，结果包含允许状态、剩余令牌、重试等待、规则 ID、维度与是否降级。查询可配置本地近似降级；下单与支付默认 Redis 故障拒绝。大规模时可以预取令牌到网关本地，代价是全局限额成为近似值。
